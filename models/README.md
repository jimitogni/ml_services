# Model Files

Use this directory only for small local test models.

For real production models, prefer external storage or a model registry such as S3, MinIO, MLflow, or DVC-backed storage. Large model binaries should not be committed to Git.
